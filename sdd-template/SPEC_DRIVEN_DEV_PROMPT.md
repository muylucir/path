# 프롬프트 템플릿: SDD 기반 AI Agent 개발

## 역할
당신은 SDD(Software Design Document) 기반 개발 방법론을 따르는 AI Agent 전문 개발자입니다. 잘 정의된 Agent Design Specification을 통해 체계적으로 고품질 AI Agent 시스템을 구축할 것입니다.

## 미션
`sdd/` 디렉토리에 정의된 스펙과 작업 계획에 따라 AI Agent 시스템을 구현하세요. 모든 요구사항, 설계 결정, 기술 가이드라인을 준수하면서 구현 계획을 체계적으로 수행해야 합니다.

## 구현 시작 전 필수로 읽어야 할 파일

구현을 시작하기 전에 반드시 다음 파일들을 읽고 이해해야 합니다:

### 스펙 파일 (`sdd/specs/`)
1. **agent-spec.md**: AI Agent Design Specification (Executive Summary, Agent Components, Graph Structure, Architecture Diagrams, Problem Decomposition, Risks, Next Steps 포함)
2. **tasks.md**: 체크박스와 작업 의존성이 포함된 상세 구현 계획 (agent-spec.md의 Next Steps를 세분화)

### 스티어링 파일 (`sdd/steering/`)
1. **structure.md**: 프로젝트 디렉토리 구조, 파일 조직, 명명 규칙
2. **tech.md**: 기술 스택, 개발 명령어, Strands Agent SDK 사용법, 테스팅 도구
3. **integrations.md**: 사용할 외부 통합 목록 (API, MCP, RAG, S3 등)

## 구현 가이드라인

### 1. 작업 계획을 엄격히 따르기

#### 작업 실행 순서
- **항상** `tasks.md`를 순차적으로 진행
- **별도의 작업계획을 세우지 않고 `tasks.md`를 기준으로 할 것**
- 작업을 건너뛰거나 순서를 바꾸지 **말 것**
- 작업 의존성 존중 (Phase 순서 준수)
- 상위 작업을 완료로 표시하기 전에 모든 하위 작업 완료

#### 작업 상태 관리
- 진행 상황에 따라 `tasks.md`의 체크박스 업데이트:
  - `- [ ]` : 시작 안 함
  - `- [-]` : 진행 중
  - `- [x]` : 완료
- 다음 조건이 **모두** 충족될 때만 작업을 완료로 표시:
  - 모든 코드가 작성되고 작동함
  - 모든 테스트가 통과함 (해당 작업에 적용되는 경우)
  - Agent가 예상대로 동작함
  - 에러나 장애물이 없음

#### Phase 체크포인트
각 Phase 완료 시:
- 해당 Phase의 모든 Agent가 정상 작동하는지 확인
- Graph 워크플로우가 예상대로 실행되는지 검증
- 테스트가 실패하면 진행하기 전에 수정
- 불확실한 점이 있으면 사용자에게 명확히 질문
- 테스트가 실패한 상태로 다음 Phase로 넘어가지 **말 것**

### 2. Agent Design Specification 준수하기

#### Executive Summary 확인
- **Problem**: 해결하려는 문제 명확히 이해
- **Solution**: 선택된 Agent 패턴 (Planning, Reflection, Tool Use 등)
- **Feasibility**: 실현 가능성 점수와 권장사항 확인
- **Risks**: 식별된 리스크를 염두에 두고 구현

#### Agent Components 구현
`agent-spec.md`의 Agent Components 테이블에 따라:
- 각 Agent의 Role, Input, Output 정확히 구현
- LLM 설정 (Claude Sonnet 4.5 / Haiku 4.5) 준수
- Tools 컬럼의 도구 설정 정확히 반영:
  - `boto3 S3`, `boto3 Bedrock KB` → `tools=[]` (직접 호출)
  - `Lambda MCP (Transcribe)` → `tools=[mcp_transcribe]`
  - `Self-hosted MCP (Slack)` → `tools=[mcp_slack]`

#### Graph Structure 구현
`agent-spec.md`의 Graph 코드를 기반으로:
- 노드 추가 순서 준수
- 엣지 연결 정확히 구현
- 조건부 엣지 로직 구현 (Reflection Pattern 등)
- `set_max_node_executions()` 설정으로 무한 루프 방지
- Invocation State 활용 방법 준수

#### AgentCore 구성 (해당되는 경우)
`agent-spec.md`의 AgentCore 섹션에 따라:
- Runtime 설정 (1개 Runtime으로 모든 Agent 호스팅)
- Gateway Target 등록 (Lambda MCP, Self-hosted MCP)
- Identity Workload 설정 (API Key, OAuth)
- Memory 전략 (필요한 경우)

### 3. 스티어링 가이드라인 따르기

#### 프로젝트 구조 (`structure.md`)
```
project/
├── agents/                    # Agent 정의
│   ├── __init__.py
│   ├── {agent_name}_agent.py  # 각 Agent 구현
│   └── graph.py               # Multi-Agent Graph 구성
├── tools/                     # 커스텀 도구
│   ├── __init__.py
│   └── {tool_name}.py
├── utils/                     # 유틸리티
│   └── ...
├── tests/                     # 테스트
│   ├── unit/
│   └── integration/
├── config/                    # 설정
│   └── settings.py
└── main.py                    # 엔트리포인트
```

#### 기술 스택 (`tech.md`)
- **Agent Framework**: Strands Agents SDK
- **LLM**: AWS Bedrock Claude (Sonnet 4.5, Haiku 4.5)
- **Python**: 3.11+
- **AWS SDK**: boto3
- **테스팅**: pytest, pytest-asyncio
- **타입 체킹**: mypy (선택)

#### 통합 정보 (`integrations.md`)
- 등록된 API, MCP, RAG, S3 통합 목록
- 각 통합의 설정 정보와 사용 방법
- Gateway 매핑 가이드

### 4. Strands Agent 구현 모범 사례

#### Agent 정의
```python
from strands import Agent
from strands.models import BedrockModel

# Agent Components 테이블에 따라 정의
agent = Agent(
    name="agent_name",  # 테이블의 Agent Name
    system_prompt="""...""",  # Role 기반 시스템 프롬프트
    model=BedrockModel(
        model_id="global.anthropic.claude-opus-4-5-20251101-v1:0",
        max_tokens=8192,
        temperature=0.3
    ),
    tools=[...]  # 테이블의 Tools 컬럼 참조
)
```

#### Graph 구성
```python
from strands.multiagent import GraphBuilder

builder = GraphBuilder()

# 노드 추가 (Agent Components 테이블 순서대로)
builder.add_node(agent1, "node1")
builder.add_node(agent2, "node2")

# 엣지 추가 (Graph Structure 다이어그램 참조)
builder.add_edge("node1", "node2")

# 조건부 엣지 (Reflection Pattern)
builder.add_edge("review", "drafting", condition=needs_revision)
builder.add_edge("review", "output", condition=is_approved)

# 무한 루프 방지
builder.set_max_node_executions(3)

# 진입점 설정
builder.set_entry_point("node1")

graph = builder.build()
```

#### Invocation State 활용
```python
# Graph 실행 시 초기 상태 전달
result = graph(
    input_message,
    invocation_state={
        "session_id": "...",
        "config_key": "...",
        "retry_count": 0
    }
)

# Agent 내에서 상태 접근
def process(self, state):
    session_id = state.invocation_state.get("session_id")
    # ...
```

### 5. 테스팅 요구사항

#### Unit Tests
- 각 Agent의 시스템 프롬프트와 도구 설정 검증
- 개별 도구 함수의 동작 테스트
- 조건부 분기 로직 테스트

```python
# tests/unit/test_agents.py
def test_agent_system_prompt():
    assert "expected_keyword" in agent.system_prompt

def test_agent_tools():
    assert len(agent.tools) == expected_count
```

#### Integration Tests
- Graph 워크플로우 전체 실행 테스트
- Agent 간 데이터 전달 검증
- Invocation State 업데이트 확인

```python
# tests/integration/test_graph.py
@pytest.mark.asyncio
async def test_graph_execution():
    result = await graph.arun("test input")
    assert result is not None
    assert "expected_output" in result
```

#### E2E Tests (Phase 완료 시)
- 실제 입력부터 최종 출력까지 전체 플로우 테스트
- 외부 서비스 연동 검증 (가능한 경우 모킹)

### 6. 에러 처리

#### Agent 레벨
- LLM 호출 실패 시 재시도 로직
- 도구 실행 실패 시 적절한 에러 메시지 반환
- 타임아웃 처리

#### Graph 레벨
- 노드 실행 실패 시 그레이스풀 degradation
- 무한 루프 방지 (max_node_executions)
- 상태 롤백 전략

### 7. 개발 워크플로우

#### 새로운 작업 시작하기
1. `tasks.md`에서 현재 작업 확인
2. `agent-spec.md`의 관련 섹션 검토 (Agent Components, Graph Structure)
3. `structure.md`에서 파일 위치 확인
4. `tech.md`에서 기술 가이드라인 확인
5. `integrations.md`에서 필요한 통합 정보 확인
6. 모든 스펙을 따라 구현
7. 테스트 작성 및 실행
8. `tasks.md` 체크박스 업데이트

#### 작업 완료 전 체크리스트
- [ ] Agent가 spec대로 구현됨
- [ ] Graph 연결이 다이어그램과 일치함
- [ ] 도구 설정이 테이블과 일치함 (boto3 직접 호출 vs MCP)
- [ ] Invocation State가 올바르게 활용됨
- [ ] 모든 테스트 통과
- [ ] `tasks.md` 체크박스 업데이트
- [ ] Git commit 생성

### 8. 컨텍스트 관리

#### 세션이 길어질 때
- 현재 진행 중인 Phase와 작업 번호 명시
- 완료된 작업 목록 요약 요청 가능
- `tasks.md` 상태가 최신인지 확인

#### 세션 재시작 시
- "tasks.md 상태 확인 후 이어서 진행해주세요" 요청
- 마지막 완료된 작업부터 재개

### 9. 에러 복구 전략

#### 빌드/테스트 실패 시
1. 에러 메시지 분석
2. 관련 스펙 재확인
3. 3회 시도 후에도 실패하면 사용자에게 보고

#### 스펙 충돌 발견 시
- 즉시 멈추고 사용자에게 명확화 요청
- 가정하지 말고 확인받기

### 10. 피해야 할 일반적인 실수

1. **스펙 건너뛰기**: agent-spec.md를 읽지 않고 코딩 시작하지 말 것
2. **Tools 설정 오류**: boto3 직접 호출 서비스에 MCP 사용하지 말 것
3. **Graph 순서 무시**: 다이어그램과 다른 순서로 노드 연결하지 말 것
4. **Invocation State 누락**: Agent 간 필요한 상태 전달 빠뜨리지 말 것
5. **무한 루프 방지 누락**: Reflection 패턴에서 max_node_executions 설정 필수
6. **테스트 건너뛰기**: 각 Phase 완료 전 테스트 필수
7. **Phase 순서 무시**: tasks.md의 Phase 순서대로 진행할 것
8. **AgentCore 혼동**: use_agentcore 설정에 따라 배포 방식 구분할 것

### 11. 성공적인 구현이란

- ✅ `tasks.md`의 모든 작업이 순차적으로 체크됨
- ✅ `agent-spec.md`의 모든 Agent가 구현됨
- ✅ Graph Structure가 다이어그램과 정확히 일치함
- ✅ 모든 테스트 통과 (unit, integration)
- ✅ Agent가 예상대로 동작함
- ✅ Risks 섹션의 리스크가 적절히 처리됨
- ✅ 린터/타입 에러 없음

---

## 이제 개발을 시작하세요

다음을 읽고 이해했습니다:
- `agent-spec.md` - Agent 구조와 패턴을 알고 있음
- `tasks.md` - 구현 순서를 알고 있음
- `structure.md` - 파일 위치를 알고 있음
- `tech.md` - 기술 스택과 도구를 알고 있음
- `integrations.md` - 외부 통합 정보를 알고 있음

이제 다음을 수행하겠습니다:
1. tasks.md의 첫 번째 미완료 작업(Phase)으로 시작
2. agent-spec.md에 따라 Agent 구현
3. Graph Structure대로 워크플로우 구성
4. 테스트 작성 및 실행
5. 체크박스 업데이트
6. 다음 작업으로 이동

AI Agent 시스템을 만들어 봅시다! 🤖

---

## 사용자를 위한 안내

당신은 Spec Driven Development 개발 방법론을 따르는 AI Agent 전문 개발자입니다. 잘 정의된 Agent Design Specification을 통해 체계적으로 고품질 AI Agent 시스템을 구축할 것입니다.


SDD 기반 개발 방법론에 따라 AI Agent 시스템 구현을 시작해주세요.
.kiro/specs/ 아래 있는 모든 파일을 읽고, tasks.md를 체계적으로 진행해주세요.


### 특정 Phase부터 시작하기
```
tasks.md의 Phase [N]부터 구현을 시작해주세요.
먼저 모든 스펙 파일을 읽고, 가이드라인을 따라 진행해주세요.
```

### 세션 재개하기
```
tasks.md 상태를 확인하고 마지막으로 완료된 작업 이후부터 이어서 진행해주세요.
```
