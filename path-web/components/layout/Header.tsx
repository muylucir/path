"use client";

// Header is now integrated into AppLayoutShell via TopNavigation.
// This file is kept as an empty export for backwards compatibility during migration.
// Remove after all pages use AppLayoutShell.

export function Header() {
  return null;
}
